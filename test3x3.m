%% Parameters.
A = [8     1     6;
     3     5     7;
     4     9     2;];

alpha = 0.1;
beta = -0.1;
gamma = -0.1;

[n, m] = size(A);

x = zeros(n,1);
y = zeros(n,1);

%% Utils


%% First diagonal element.
 
 